planId: real-032
intent: CalculateAverageDaysInvoiceIssue
agentId: SalesAgent
domain: sales
displayName: Sales Agent
capabilities:
  - sales-invoices
  - sales-get-invoice
understanding: "Calculate the average number of days from invoice issue date to payment date across all customers over the ${period}"
description: "Fetch all paid sales invoices from the ${period} (${date} to ${date}) with status=Collected, extract payment dates from the payments array in each invoice detail, calculate days-to-collect per invoice (payment_date - issue_date), compute the average across all invoices, and return as Text Summary with the metric and supporting context"
expectedData:
  summary:
    - AverageDaysToCollect
    - InvoiceCount
    - PeriodFrom
    - PeriodTo
    - PeriodLabel
defaultEntities:
  companyId: DEMO-001
  dateFrom: "2026-02-01"
  dateTo: "2026-05-01"
sampleQueries:
  - "Calculate the average number of days from invoice issue date to payment date across all customers over the ${period}"
  - "Average number of days from invoice issue date to payment date across all?"
steps:
  - id: 1
    action: sales-list-sales-invoices
    stepType: Tool
    toolName: sales_list_sales_invoices
    toolId: "f5fc423c-2544-084c-56df-09d849ac1737"
    stepReason: "Fetch paid invoices last 3 months"
    dependsOn: []
    outputFields: [invoices]
    input:
      - key: status
        value: "[\"Collected\"]"
      - key: date_from
        value: "2026-02-01"
      - key: date_to
        value: "2026-05-01"
      - key: page_size
        value: "200"
    nextStep: 2
  - id: 2
    action: compute-calculate-average-days-to-collect
    stepType: Code
    functionName: calculateDSO
    stepReason: "Calculate average days to collect"
    dependsOn: [1]
    outputFields: [averageDaysToCollect, invoiceCount, periodFrom, periodTo, periodLabel]
